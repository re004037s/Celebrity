<%= form_for(current_user.html_css_status, remote: true) do |f| %>
  <%= hidden_field_tag :course, course %>
  <%= hidden_field_tag :status, false %>
  <%= f.submit '未完了', class: 'txt3 btn btn-lg btn-incomp' %>
<% end %>