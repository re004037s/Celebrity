<%= form_for(current_user.railstutorial_status, remote: true) do |f| %>
  <%= hidden_field_tag :course, course %>
  <%= hidden_field_tag :status, false %>
  <%= f.submit '未完了', class: 'btn btn-incomp' %>
<% end %>