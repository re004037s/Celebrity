<meta name="viewport" content="width=device-width; initial-scale=1.0">
<div class="main-wrapper container">
  <div class="row">
    <div class="col-sm-9">
      
    <% if current_user.feedbacks.find_by(movie_id: 4) %>
      <div id="m5">
        <%= render 'shared/movie', movies: @movies, movie_id: 5 %>
        <%= render 'feedbacks/form', feedback: @feedback, movie_id: 5 %>
      </div>
    <% end %>

    <% if current_user.feedbacks.find_by(movie_id: 5) %>
      <div id="m6">
        <%= render 'shared/movie', movies: @movies, movie_id: 6 %>
        <%= render 'feedbacks/form', feedback: @feedback, movie_id: 6 %>
      </div>
    <% end %>

    <% if current_user.feedbacks.find_by(movie_id: 6) %>
      <div id="m7">
        <%= render 'shared/movie', movies: @movies, movie_id: 7 %>
        <%= render 'feedbacks/form', feedback: @feedback, movie_id: 7 %>
      </div>
    <% end %>

      <% if current_user.feedbacks.find_by(movie_id: 7) %>
        <p class="text-center">
          <%= link_to "NEXT：就活編", job_hunting_path, class: "txt1 btn btn-lg btn-primary" %>
        </p>
      <% end %>
      
    </div>
    <%= render 'layouts/aside' %>
  </div>
</div>